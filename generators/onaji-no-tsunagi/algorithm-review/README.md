# 人間の目によるアルゴリズムレビュー

この文書を、多数問監査レポートの作成、表示、更新に関する正本とする。

機械検査で得た集計値と問題標本を一覧表示し、人間が盤面を見ながら
生成アルゴリズムとfilter条件をレビューする。自動分類は比較の補助であり、
問題の美しさ、児童の体感難易度、児童利用可否を証明するものではない。

## ディレクトリの役割

### 参照テンプレート

`../../../../ref/human-algorithm-review-report/` は、新しいレポート実体を作るときに
参照する表示テンプレートである。これは作業用の参照資産であり、監査のために
変更しない。

```text
ref/human-algorithm-review-report/
├── template.html
├── template.css
└── template.js
```

ここには監査結果JSON、監査ごとの集計値、起動スクリプト、別のREADMEを置かない。
テンプレートの変更が必要に見える場合も、先にこのGit管理下のレビュー実体で
要件と回帰を確認し、参照テンプレートには明示的な変更依頼があるまで触れない。

### 現在のレポート実体

このディレクトリは、現在レビューしているレポート基盤を保持するGit管理対象である。
監査JSONはローカルで生成・更新する作業データであり、`data/.gitignore` により
Git管理しない。

```text
algorithm-review/
├── README.md
├── server.sh
├── index.html
├── style.css
├── app.js
└── data/
    ├── .gitignore
    └── *.json                         # ローカル生成物（Git管理しない）
```

- `index.html`: レポートの表示領域と既定JSONパス
- `style.css`: 採用したB3デザインと盤面一覧・モーダルのレイアウト
- `app.js`: JSON読込、集計表、SVG盤面、問題モーダルの描画
- `data/*.json`: 監査処理が生成した入力データ（Git管理しないローカル生成物）
- `server.sh`: このディレクトリだけをlocalhost配信する起動スクリプト

表示コードと監査データを分離し、集計値や問題座標をHTMLへ直書きしない。

## 起動する

このディレクトリで次を実行する。

```bash
./server.sh
```

ブラウザで次を開く。既定では6×6・12端点を2,000問生成した監査JSONを
表示する。

```text
http://127.0.0.1:8000/
```

`server.sh` は `127.0.0.1:8000` にだけbindし、このディレクトリより上は
配信しない。停止するときは、起動したterminalで `Ctrl+C` を押す。

## 入力JSONを切り替える

既定では、`index.html` の `data-default-report` で指定した
`data/6x6-4-4-4-filter-classification-audit-2000.json` を読み込む。

別の監査JSONを一時的に表示する場合は、`data/` に配置して `report` queryへ
ファイル名を指定する。

```text
http://127.0.0.1:8000/?report=別ファイル.json
```

`report` は `data/` 内のJSONファイル名だけを受け付ける。ディレクトリ区切り、
`..`、JSON以外の拡張子は拒否する。

既定の監査を変更する場合は、次のどちらかを行う。

1. 新しい監査結果で既定JSONを置き換える。
2. JSONを内容が分かる別名で保存し、`index.html` の
   `data-default-report` を変更する。

多数問監査の入力JSONは、現在
`onaji-no-tsunagi.six-by-six-filter-classification-audit.v3` を使用する。
新しいschemaを導入するときは、旧schemaを黙って読み替えず、`app.js` の
入力検査とこの文書を同じ変更で更新する。

## localhost配信を使う理由

`file://` で開いたHTMLから別ファイルのJSONを `fetch` すると、
ブラウザの同一オリジン制約により失敗する。ファイル選択やドロップ操作を
毎回要求せず、HTML・CSS・JavaScriptとJSONを分離したまま自動表示するため、
localhost配信を標準とする。

## 表示上のレビュー契約

- 条件別の違反問題数と、各違反群の難易度分類内訳を同じ表で比較できる。
- 表の末尾に、filter適用前の母集団と全filter通過群を表示する。
- 条件一覧のコピーボタンは、各条件をMarkdownの `* ` 箇条書きでコピーする。
- 問題カードは一覧性を優先して問題盤面だけを表示する。
- 問題ID、答え、機械指標、違反条件は問題カードのモーダル内に表示する。
- 該当問題が0問の分類も省略せず、「該当問題なし」と表示する。
- 原本問題を表示する場合も、監査JSONから読み込み、テンプレートへ座標を埋め込まない。

## 6x6-4-4-4のcover範囲比較

36マスcover 2,000問の監査は次を開く。

```text
http://127.0.0.1:8000/?report=6x6-4-4-4-filter-classification-audit-2000.json
```

31〜35マスcover 2,000問の監査は次を開く。

```text
http://127.0.0.1:8000/?report=6x6-4-4-4-partial-cover-filter-classification-audit-2000.json
```

31マスcover固定・分類policy v3・中央端点数3〜5個・直線gate組み込み前の
1,000問履歴は次を開く。

```text
http://127.0.0.1:8000/?report=6x6-4-4-4-31-cell-filter-classification-audit-1000.json
```

中央端点数4〜6個・12端点の直線gateを36マス基盤と31マス完成候補の両方へ
適用した現行1,000問監査は次を開く。

```text
http://127.0.0.1:8000/?report=6x6-4-4-4-31-cell-filter-classification-audit-central-4-to-6-1000.json
```

36マス基盤だけへ直線gateを適用し、31マス化後には再適用していなかった
88問違反版は比較履歴として次に残す。

```text
http://127.0.0.1:8000/?report=6x6-4-4-4-31-cell-filter-classification-audit-base-gate-only-1000.json
```

各レポートは同じ原本基準、同じ5分類、同じ集計schemaを使う。
36マス、31〜35マス、旧31マス固定2,000問は6つの端点配置条件を使う。
31マス固定1,000問は、解答形状を評価する
`真横線3本以上または真縦線3本以上を禁止`と、端点配置を評価する
`縦横隣接で連なる端点4個以上を禁止`を加えた8条件を使う。
部分cover側は、36マス唯一解を1〜5マス短縮した後、配置filterをかけずに
独立solverで`exact: 1`を再証明し、31〜35マスかつtopology非重複の2,000問を
母集団とする。これにより、生成時に同じfilterを先取りせずcover範囲の差を測る。
31マス固定側は短縮数を5マスに固定し、同じ受入条件で1,000問を集める。

36マス、31〜35マス、旧31マス固定2,000問は分類policy v1による履歴値である。
cover範囲の差を比較した当時の集計は次のとおりである。

| 指標 | 36マス | 31〜35マス | 旧31マス固定 |
| --- | ---: | ---: | ---: |
| 原本近傍 | 1（0.05%） | 64（3.2%） | 420（21.0%） |
| 明らかに簡単側 | 73 | 186 | 243 |
| 明らかに難しい側 | 0 | 0 | 5 |
| 指標混合・簡単寄り | 1,908 | 1,623 | 1,094 |
| 指標混合・難しい寄り | 18 | 127 | 238 |
| 全filter通過 | 775（38.75%） | 812（40.6%） | 703（35.15%） |
| 通過群の原本近傍 | 0 | 24（2.96%） | 172（24.47%） |
| 初期選択肢量・中央値 | 124,416 | 373,248 | 1,679,616 |
| 強制出口・中央値 | 3 | 2 | 1 |
| solver状態・中央値 | 214 | 210 | 188 |
| 総曲がり・中央値 | 10 | 8 | 6 |

変更前の31マス固定監査は、同じ1,000問についてpolicy変更前後の分類遷移をJSONへ
保存する。policy v2は、簡単方向が2指標以上かつ難しい方向より多い場合に
難しい方向1指標までを許容し、直線的で簡単すぎる問題を
`明らかに簡単側`へ移す。難しい側の条件は広げない。

| 分類 | 同一1,000問 v1 | 同一1,000問 v2 | 差 |
| --- | ---: | ---: | ---: |
| 明らかに簡単側 | 124（12.4%） | 240（24.0%） | +116 |
| 原本近傍 | 194（19.4%） | 194（19.4%） | 0 |
| 明らかに難しい側 | 4（0.4%） | 4（0.4%） | 0 |
| 指標混合・簡単寄り | 549（54.9%） | 434（43.4%） | -115 |
| 指標混合・難しい寄り | 129（12.9%） | 128（12.8%） | -1 |

遷移は混合指標から明らかに簡単側への116問だけで、原本近傍と難しい側は
変化しなかった。

policy v3は、canonical solutionに曲がり0の真横経路が3本以上、または
曲がり0の真縦経路が3本以上あれば、相対4指標より優先して
`明らかに簡単側`とする。横と縦は合算しない。

| 分類 | 同一1,000問 v2 | 同一1,000問 v3 | 差 |
| --- | ---: | ---: | ---: |
| 明らかに簡単側 | 240（24.0%） | 313（31.3%） | +73 |
| 原本近傍 | 194（19.4%） | 162（16.2%） | -32 |
| 明らかに難しい側 | 4（0.4%） | 4（0.4%） | 0 |
| 指標混合・簡単寄り | 434（43.4%） | 395（39.5%） | -39 |
| 指標混合・難しい寄り | 128（12.8%） | 126（12.6%） | -2 |

直線gate組み込み前は、直線filterの違反が133問（13.3%）あり、全133問が
`明らかに簡単側`だった。
端点4個以上の連結filterは、端点セルを縦横隣接だけで連鎖的にたどれる
同一連結成分を対象とする。途中で曲がるL字・階段状も含み、斜め隣接、
3端点と孤立1端点、離れた2端点組が2つの配置は含まない。
違反は386問（38.6%）で、他の7条件をすべて通過していた90問を追加で除外した。
条件別JSONには、各違反群の原本近傍から決定的な擬似ランダム抽出で最大30問を
選び、抽出後に原本距離順へ並べた`referenceLikeReviewCandidates`を保持する。

中央端点数3〜5個だった変更前は、8条件の全filter通過が234問（23.4%）、
通過群の原本近傍は
46問（19.66%）である。直線filterは、他の7条件をすべて通過していた
39問を独立に除外した。

中央端点数を4〜6個へ変更した同一母集団では、全filter通過が370問（37.0%）
だった。その後、12端点の36マス基盤生成へ直線gateを組み込み、同じseed系列から
31マスへ短縮し直した履歴母集団は次のように変化した。基盤生成時のpolicy IDと
条件IDはJSONの`sampling.basePuzzleSelectionPolicyAppliedDuringGeneration`へ
保存する。短縮後にcanonical solutionが変わるため、基盤だけのgateでは直線違反が
88問残った。

| 指標 | gate組み込み前 | 基盤だけに組み込み | 差 |
| --- | ---: | ---: | ---: |
| 明らかに簡単側 | 313 | 274 | -39 |
| 原本近傍 | 162 | 165 | +3 |
| 明らかに難しい側 | 4 | 4 | 0 |
| 指標混合・簡単寄り | 395 | 420 | +25 |
| 指標混合・難しい寄り | 126 | 137 | +11 |
| 直線filter違反 | 133（13.3%） | 88（8.8%） | -45（-4.5pt） |
| 全8条件通過 | 370（37.0%） | 393（39.3%） | +23（+2.3pt） |
| 通過群の原本近傍 | 75 | 76 | +1 |

現行監査では、独立solverで31マス候補のcanonical solutionを確定した後、
同じprofile policyを再適用してから採用する。最終候補へのpolicy IDと条件IDは
JSONの`sampling.transformedPuzzleSelectionPolicyAppliedBeforeAcceptance`へ
保存する。基盤だけにgateを適用した履歴母集団との変化は次のとおりである。

| 指標 | 基盤gateのみ | 基盤＋31マス最終gate | 差 |
| --- | ---: | ---: | ---: |
| 明らかに簡単側 | 274 | 203 | -71 |
| 原本近傍 | 165 | 179 | +14 |
| 明らかに難しい側 | 4 | 4 | 0 |
| 指標混合・簡単寄り | 420 | 461 | +41 |
| 指標混合・難しい寄り | 137 | 153 | +16 |
| 直線filter違反 | 88（8.8%） | 0（0.0%） | -88（-8.8pt） |
| 全8条件通過 | 393（39.3%） | 420（42.0%） | +27（+2.7pt） |
| 通過群の原本近傍 | 76 | 84 | +8 |

基盤gateのみの1,000問は332基盤へ66,252変形を試し、変形重複2,442件と
非唯一解62,810件を除いて得た。全問が31マス、独立solverの`exact: 1`、
topology非重複であり、integrity checkは全項目を通過した。これは機械分類と
filter通過率の変化であり、児童の正答率や体感難易度の改善を意味しない。

最終gate適用後の現行1,000問は385基盤へ76,836変形を試し、非唯一解72,823件、
31マス正準解のprofile policy違反88件、変形重複2,925件を除いて得た。
直線filter違反は0問で、全問が31マス、独立solverの`exact: 1`、
topology非重複であり、integrity checkは全項目を通過した。

31〜35マス側の内訳は、31マス31問、32マス79問、33マス232問、
34マス525問、35マス1,133問である。
旧31マス固定2,000問は、674基盤へ134,684変形を試し、変形重複5,320件と
非唯一解127,364件を除いて得た。直線gate組み込み前の1,000問は、
324基盤へ64,765変形を試し、
変形重複2,394件と非唯一解61,371件を除いて得た。solver予算超過は0件で、
全問が31マス、`exact: 1`、topology非重複である。変形試行からの採用率は
現行1,000問で1.30%であり、31〜35マス側の15.88%より大幅に低い。
人間レビュー標本は5表示区分から、候補IDに基づく決定的な擬似ランダム抽出で
最大30問ずつ選ぶ。抽出後だけ原本距離または難易度方向で表示順を整える。
分類内の母数が30問未満なら存在する全問を表示し、不足分は未選択候補から
同じ方法で補完する。filter適用前と全filter通過をそれぞれ最大150問にする。

部分cover監査を再生成する場合は次を使う。HTML、JavaScript、CSSは変更せず、
既存schemaに適合するJSONだけを更新する。

```bash
cd ../../..
npm run audit:onaji-no-tsunagi:6x6-partial-cover-filter-classification -- \
  --sample-count=2000 \
  --output-prefix=generators/onaji-no-tsunagi/algorithm-review/data/6x6-4-4-4-partial-cover-filter-classification-audit-2000 \
  --write-html=false
```

31マス固定監査の再生成コマンドは次である。

```bash
cd ../../..
npm run audit:onaji-no-tsunagi:6x6-31-cell-filter-classification
```

機械分類の増減は、美しさや児童の体感難易度の証拠ではない。

## 新しい監査レポートを作る

1. 対象profileと母集団、seed系列、filter条件を決める。
2. 監査処理から、集計とレビュー標本を含むJSONを生成する。
3. JSONを内容が分かるファイル名で `data/` に配置し、必要なら
   `data-default-report` を変更する。
4. `server.sh` を起動して、集計値がJSONと一致することを確認する。
5. 実ブラウザで、表、問題カード、問題・答えモーダル、IDコピーを確認する。
6. consoleのwarning/error、狭幅表示、長い条件名や0問分類の表示を確認する。

自動検査を通過しても、人間による問題標本の確認が終わったことにはしない。

`data/*.json` はGit管理しない。既定JSONを含め、clone直後や別環境では上記の
再生成コマンドで必要な監査JSONを配置してから表示する。

## 表示基盤を変更するとき

`index.html`、`style.css`、`app.js` はこのGit管理下のレビュー実体である。
これらはユーザーから明示的な変更依頼がない限り変更しない。監査固有の集計値、
条件、問題標本だけが変わる場合は、既存schemaに適合する入力JSONだけを更新する。

外側の `ref/human-algorithm-review-report/` は不変の参照テンプレートとして扱う。
そのテンプレートを更新する作業は、このレビュー基盤の変更とは別途、明示的に依頼
された場合だけ行う。
