# 算数教材ジェネレータ 作業ガイド

## 適用範囲

このファイルはリポジトリ全体に適用する。
配下に別の `AGENTS.md` がある場合は、そのディレクトリでは配下の指示も優先して適用する。

## 作業前に確認するもの

- リポジトリ全体の構成と実行方法は `README.md` を確認する。
- ディレクトリ設計を変える場合は `docs/directory-structure-proposal.md` を確認する。
- 教材を変更する場合は、対象の `generators/<generator-slug>/SPEC.md` を先に読む。
- 作業開始時に `git status --short` を確認し、既存の未コミット変更を上書きしない。

## 実装ルール

- 教材固有の画面、作問処理、検証処理、描画、スタイル、テスト、仕様書は、原則として同じ `generators/<generator-slug>/` 内に置く。
- 既存ジェネレータのディレクトリを丸ごと複製せず、必要なファイルだけを作る。
- 2つ以上の教材で同じ実装が実際に必要になるまで、ルートに `lib/`、`shared/`、共通CSSなどを作らない。
- 教材固有のルールを、見かけ上の重複だけを理由に共通化しない。
- 仕様を変更した場合は、`SPEC.md`、実装、テスト、必要に応じて画面上の説明を同じ作業で同期する。
- 乱数を使う作問処理は、可能な限り再現可能な入力またはseedでテストできる構造を保つ。
- 問題の生成条件だけでなく、解の成立、一意性、難易度条件など、`SPEC.md` が保証するとしている性質をテストする。
- 空の将来用ファイルや、使用されない設定項目を追加しない。

### 生成エンジンの切り出し可能性

- 各教材のエンジン部分のソースコードは、容易にnpmパッケージへ切り出せる状態を保つ。
- 作問、solver、validator、domain型、runtime入力検証などのエンジン境界から、React、DOM、CSS、Web Worker、SPAホスト、Node固有I/O、別教材へ依存しない。実行環境固有の処理はUIまたはadapter側に置く。
- npmパッケージ用の狭い公開入口から必要なAPIと型だけをexportできる構造を保ち、内部探索helperや画面都合の型を公開契約へ漏らさない。
- エンジンへruntime依存を追加する場合は、Nodeとブラウザのどちらで利用できるか、配布時に外部依存として必要か、既存の決定性とテスト可能性を損なわないかをコードレビューで確認する。
- 実際にパッケージ化する依頼がない段階では、将来用の空package、未使用の公開入口、配布設定は追加しない。

### 可読性とレビュー可能性

- コーディングでは「リーダブルコード」を意識し、人間がコードレビューで意図、制御の流れ、変更の影響範囲を追える構造を優先する。
- 名前は役割とドメイン上の意味を表すものにし、略語、曖昧な汎用名、型名や実装詳細を言い直すだけの名前を避ける。
- 関数とモジュールは責務を明確にし、過度なネスト、隠れた副作用、離れた場所で成立する暗黙の前提を減らす。
- 行数の短さや技巧的な抽象化より、明示的な制御、適切な中間変数、早期returnなど、レビュー時に判断しやすい表現を選ぶ。
- 共通化は意味と変更理由が同じ処理に限る。見た目が似ているだけのドメインルールを一つにまとめない。
- コメントはコードの動作を言い直すためではなく、採用理由、保証範囲、非自明な制約、別案を採らなかった理由を残すために使う。

### JSDoc・TSDoc

- JavaScriptではJSDoc、TypeScriptではTSDoc互換の `/** ... */` を使う。説明文は原則として日本語とし、識別子と技術用語はコードと同じ表記を使う。
- APIリファレンス対象のTypeScript実装moduleは、importより前の最初のコメントに `@packageDocumentation` を付け、ファイルの責務、層の境界、重要な保証または非保証を短く説明する。export一覧を言い直すだけの説明にはしない。
- 他moduleから利用されるexport関数、class、型のうち、名前と型だけでは利用契約を誤解し得るものにはドキュメントコメントを付ける。
- 特に、application/domainの入口、solver・validator・generator、アルゴリズム上の不変条件や枝刈り、決定性、唯一解、探索予算、状態によって意味が変わる戻り値、例外を扱うAPIでは必須とする。
- ドキュメントコメントには必要に応じて、目的、事前条件、保証することと保証しないこと、戻り値の状態、単位や範囲、副作用、例外を記載する。
- `@param`、`@returns`、`@throws`、`@remarks`、`@example` は説明が必要な場合だけ使い、型定義や関数名から明らかな内容を繰り返さない。
- feature内のmodule間で共有するためだけのexportは、それだけを理由にコメント必須とはしない。ただし、非自明なアルゴリズム、前提、不変条件を持つ場合は内部exportでも記載する。
- 単純な変換関数、明白なaccessor、型とpropsだけで役割が分かるUI componentには、追加情報のない定型コメントを付けない。
- TSDoc/JSDocは仕様書やテストの代わりにしない。契約を変更した場合は、実装、型、テスト、関連文書と同じ変更でコメントも更新し、古いコメントを残さない。
- コードレビューでは、追加・変更されたexportについてコメントの要否を確認し、必須対象の欠落と実装に合わない古いコメントを修正対象とする。

## UI・印刷物の確認

- UIや描画を変更した場合は、ローカルサーバー経由で対象ジェネレータを表示して確認する。
- 画面表示だけでなく、印刷プレビューまたは印刷向けレイアウトも確認する。
- 問題用紙と解答・解説がある場合は、双方について欠落、重なり、はみ出し、不自然な改ページがないか確認する。
- PDFを作成する作業では、グローバルガイドの `guides/pdf-creation-howto.md`（「読ませるPDFを作る How-to」）に従い、全ページを検査する。

## 検証コマンド

変更範囲に応じて、少なくとも関連する個別テストを実行する。作業完了前には原則として全体テストも実行する。

```bash
npm test
```

`kazu-sagashi` の生成品質や多数seedの確認に関係する変更では、コーパステストも実行する。

```bash
npm run test:kazu-sagashi:corpus
```

ローカル表示の確認には次を使用する。

```bash
npm run serve
```

テストを実行できなかった場合や、ブラウザ・印刷確認を省略した場合は、完了報告で理由と未確認範囲を明記する。

## 変更時の注意

- 依頼範囲と無関係なファイルを整形・改名・削除しない。
- 既存の未コミット変更が対象ファイルと重なる場合は、内容を確認して保持する。
- 削除や大きな構成変更では、参照元、import、HTMLのパス、ルート一覧、テスト入口、ドキュメントのリンクを確認する。
- 新しいジェネレータを追加した場合は、ルートの `index.html` から到達できるようにし、`README.md` の収録一覧も更新する。

## 完了条件

- 実装が対象の `SPEC.md` と一致している。
- 関連テストと `npm test` が成功している、または未実行理由が明記されている。
- UI変更はブラウザで、印刷物変更は印刷レイアウトで確認されている。
- 仕様、画面説明、テスト、リンクに取り残された古い記述がない。
- 完了報告に、変更内容、確認内容、残る制約や未確認事項を簡潔に記載している。
